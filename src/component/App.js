import Home from '../pages/Home';
import '../style/global.css';
import Layout from './Layout';

const App = function () {
    return (
        <Layout>
            <Home />
        </Layout>
    );
};

export default App;
